# ArdNative default built setting
# see https://github.com/arduino/ArduinoCore-avr/blob/master/platform.txt
compiler.c.extra_flags={compiler.warning_flags.all} -std=gnu11
# ここにinclude pathを追加すれば、拡張子なしのheaderが読めるようになる。
# -I{runtime.tools.avr-gcc.path}/
compiler.cpp.extra_flags=-{compiler.warning_flags.all} -std=gnu++17
