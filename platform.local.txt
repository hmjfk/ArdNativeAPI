# ArdNative default build setting. 
# before setting see https://raw.githubusercontent.com/arduino/ArduinoCore-avr/refs/heads/master/platform.txt
compiler.c.extra_flags={compiler.warning_flags.all}
# ここにinclude pathを追加すれば、拡張子なしのheaderが読めるようになる。
# -I{runtime.tools.compiler.path}/
compiler.cpp.extra_flags=-fno-permissive {compiler.warning_flags.all} -Werror=narrowing -std=gnu++17 -fno-rtti -fno-exceptions
recipe.hooks.core.postbuild.2.pattern="{compiler.path}{compiler.ar.cmd}" d "{build.path}/core/core.a" main.cpp.o